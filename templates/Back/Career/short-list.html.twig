<div class="row">
    <h2 class="module-title">Parcours</h2>
</div>
<div class="module-content">
    <div class="row">
        <div class="col-sm-12">
            <table class="table">
                <caption>Entreprise</caption>
                <thead>
                <tr>
                    <td>Poste</td>
                    <td>Structure</td>
                    <td>Statut</td>
                    <td>Actions</td>
                </tr>
                </thead>
                <tbody>
                {% for career in corporate %}
                    {{ include('Back/Career/short-list-array.html.twig') }}
                {% endfor %}
                </tbody>

            </table>
        </div>

        <div class="col-sm-12">
            <table class="table">
                <caption>Formation</caption>
                <thead>
                <tr>
                    <td>Poste</td>
                    <td>Structure</td>
                    <td>Statut</td>
                    <td>Actions</td>
                </tr>
                </thead>
                <tbody>
                {% for career in formation %}
                    {{ include('Back/Career/short-list-array.html.twig') }}
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
<div class="row">
    <a href="{{ path('cm_back_career_list') }}" class="btn btn-default col-xs-12">Accéder à la section</a>
</div>



